import Graphics.Element exposing (..)
import Mouse exposing (..)
import Window exposing (..)
import Keyboard exposing (..)

main =
  show "Hello, Got Lambda !"
--  show Mouse.position
--  Signal.map show Mouse.position
--  Signal.map show Window.dimensions
--  Signal.map show Keyboard.arrows
--  Signal.map show Keyboard.presses
--  Signal.may show Time.every 1000
--  Signal.may show Time.every Time.second
--  Signal.may show Time.every 1
